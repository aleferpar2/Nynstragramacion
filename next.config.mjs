/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'pw4nikjt7r7hrmxe.public.blob.vercel-storage.com',
            's.gravatar.com',
            'lh3.googleusercontent.com'
        ]
    }
};

export default nextConfig;
